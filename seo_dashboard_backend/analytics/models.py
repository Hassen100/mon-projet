"""
Analytics Models

This app doesn't require database models as it's a pure API service
that fetches data from Google Analytics. However, we keep this file
for Django app structure completeness.
"""

from django.db import models

# No database models needed for this analytics API service
# All data is fetched from Google Analytics API in real-time
